{
  "name": "bifrost",
  "private": true,
  "scripts": {
    "check": "bun test && tsc -p tsconfig.json && cd web && tsc -b",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.4",
    "@types/web-push": "^3.6.4",
    "typescript": "^5.9.0"
  },
  "dependencies": {
    "web-push": "^3.6.7"
  }
}
